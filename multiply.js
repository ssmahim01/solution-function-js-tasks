// Multiply the four numbers.

function doMultiply(num1, num2, num3, num4){
    const multiply = num1 * num2 * num3 * num4;
    console.log('The multiplication result is:', multiply);
}
doMultiply(2, 6, 10, 14);

// It returns 'The multiplication result is: 1680'